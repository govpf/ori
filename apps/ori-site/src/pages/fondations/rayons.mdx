---
layout: ../../layouts/DocLayout.astro
title: "Rayons"
current: "rayons"
headerCurrent: "tokens"
---

export const wrapStyle = {
  display: 'flex',
  flexDirection: 'column',
  gap: 32,
  padding: 24,
  fontFamily: 'Inter, system-ui, sans-serif',
  color: 'var(--color-text-primary)',
  backgroundColor: 'var(--color-surface-base)',
};

export const radii = ['none', 'sm', 'md', 'lg', 'xl', '2xl', 'full'];

<div style={wrapStyle}>

# Radii

Rayons d'arrondi des éléments (boutons, cards, inputs, dialogs…).

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(140px, 1fr))',
    gap: 16,
  }}
>
  {radii.map((r) => (
    <div key={r} style={{ textAlign: 'center' }}>
      <div
        style={{
          width: 100,
          height: 100,
          margin: '0 auto 8px',
          backgroundColor: 'var(--color-brand-primary-subtle)',
          border: '1px solid var(--color-brand-primary)',
          borderRadius: `var(--radius-${r})`,
        }}
      />
      <code style={{ fontSize: 11, color: 'var(--color-text-muted)' }}>radius-{r}</code>
    </div>
  ))}
</div>

</div>
