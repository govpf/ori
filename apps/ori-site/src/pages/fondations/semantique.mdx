---
layout: ../../layouts/DocLayout.astro
title: "Tokens sémantiques"
current: "semantique"
headerCurrent: "tokens"
---

export const wrapStyle = {
  display: 'flex',
  flexDirection: 'column',
  gap: 32,
  padding: 24,
  fontFamily: 'Inter, system-ui, sans-serif',
  color: 'var(--color-text-primary)',
  backgroundColor: 'var(--color-surface-base)',
};

export const sectionTitle = {
  fontSize: 14,
  fontWeight: 600,
  textTransform: 'uppercase',
  letterSpacing: '0.05em',
  color: 'var(--color-text-secondary)',
  margin: 0,
};

export const Swatch = ({ name, varName, hint }) => (
  <div style={{ display: 'flex', gap: 12, alignItems: 'center' }}>
    <div
      style={{
        width: 56,
        height: 56,
        borderRadius: 8,
        backgroundColor: `var(${varName})`,
        boxShadow: 'inset 0 0 0 1px var(--color-border-subtle)',
      }}
    />
    <div style={{ fontSize: 13 }}>
      <div style={{ fontWeight: 600 }}>{name}</div>
      <div style={{ color: 'var(--color-text-muted)', fontFamily: 'monospace', fontSize: 11 }}>
        {varName}
      </div>
      {hint && <div style={{ color: 'var(--color-text-muted)', fontSize: 11 }}>{hint}</div>}
    </div>
  </div>
);

export const Group = ({ title, tokens }) => (
  <section>
    <h3 style={sectionTitle}>{title}</h3>
    <div
      style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fill, minmax(260px, 1fr))',
        gap: 12,
        marginTop: 12,
      }}
    >
      {tokens.map(([name, varName, hint]) => (
        <Swatch key={varName} name={name} varName={varName} hint={hint} />
      ))}
    </div>
  </section>
);

<div style={wrapStyle}>

# Alias sémantiques

C'est ce qu'un composant consomme. Ces alias **changent selon le thème
(clair / sombre)** ; utilisez-les pour garantir la portabilité du composant
entre thèmes.

<Group
  title="Marque"
  tokens={[
    ['brand / primary', '--color-brand-primary'],
    ['brand / primary-hover', '--color-brand-primary-hover'],
    ['brand / primary-active', '--color-brand-primary-active'],
    ['brand / primary-subtle', '--color-brand-primary-subtle'],
    ['brand / on-primary', '--color-brand-on-primary', 'texte sur fond primary'],
  ]}
/>

<Group
  title="Surfaces"
  tokens={[
    ['surface / base', '--color-surface-base'],
    ['surface / muted', '--color-surface-muted'],
    ['surface / subtle', '--color-surface-subtle'],
    ['surface / inverse', '--color-surface-inverse'],
  ]}
/>

<Group
  title="Texte"
  tokens={[
    ['text / primary', '--color-text-primary'],
    ['text / secondary', '--color-text-secondary'],
    ['text / muted', '--color-text-muted'],
    ['text / inverse', '--color-text-inverse'],
    ['text / disabled', '--color-text-disabled'],
    ['text / link', '--color-text-link'],
  ]}
/>

<Group
  title="Bordures"
  tokens={[
    ['border / subtle', '--color-border-subtle'],
    ['border / default', '--color-border-default'],
    ['border / strong', '--color-border-strong'],
    ['border / focus', '--color-border-focus'],
  ]}
/>

<Group
  title="Feedback"
  tokens={[
    ['feedback / success', '--color-feedback-success'],
    ['feedback / warning', '--color-feedback-warning'],
    ['feedback / danger', '--color-feedback-danger'],
    ['feedback / info', '--color-feedback-info'],
  ]}
/>

</div>
