---
layout: ../../layouts/DocLayout.astro
title: "Ombres"
current: "ombres"
headerCurrent: "tokens"
---

export const wrapStyle = {
  display: 'flex',
  flexDirection: 'column',
  gap: 32,
  padding: 24,
  fontFamily: 'Inter, system-ui, sans-serif',
  color: 'var(--color-text-primary)',
  backgroundColor: 'var(--color-surface-muted)',
};

export const shadows = ['sm', 'md', 'lg', 'xl'];

<div style={wrapStyle}>

# Shadows

Ombres portées pour la profondeur et la hiérarchie visuelle.

<div
  style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))',
    gap: 32,
    padding: 32,
  }}
>
  {shadows.map((s) => (
    <div key={s} style={{ textAlign: 'center' }}>
      <div
        style={{
          width: 120,
          height: 120,
          margin: '0 auto 8px',
          backgroundColor: 'var(--color-surface-base)',
          borderRadius: 8,
          boxShadow: `var(--shadow-${s})`,
        }}
      />
      <code style={{ fontSize: 11, color: 'var(--color-text-muted)' }}>shadow-{s}</code>
    </div>
  ))}
</div>

</div>
